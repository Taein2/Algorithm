#include <iostream>

using namespace std;

char roma[7] = {'M','D','C','L','X','V','I'};
int num[7] = {1000,500,100,50,10,5,1};
int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string first, second;
    cin >> first;
    cin >> second;
    int result = 0;

    return 0;
}