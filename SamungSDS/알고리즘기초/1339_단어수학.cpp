#include <iostream>
#include <string>
#include <vector>
using namespace std;
vector<string> v;
int result = 0;
void f(){
    
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    string str;
    for(int i=0; i<n; i++){
        cin >> str;
        v.push_back(str);
    }
    f();
    cout << result;
    return 0;
}