#include <iostream>

using namespace std;
int k,n;
int sosu[101];
int dp[100001];
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> n >> k;
    for(int i=0; i<n; i++){
        cin >> sosu[i];
    }
    int cnt = 0, result = 0;
    while(cnt != k){
        for(int i=0; i<n; i++){
            
            cnt++;
        }
    }
    return 0;
}